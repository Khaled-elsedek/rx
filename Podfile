project 'observable-withUIElement.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'observable-withUIElement' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for observable-withUIElement
  pod 'RxSwift'
  pod 'RxCocoa'

end
